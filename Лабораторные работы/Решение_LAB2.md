# Решение Лабораторная работа №2 "Оценка и коррекция траектории БПЛА по визуальным меткам ArUco"

## Описание решения

Студенту нужно будет самому запускать файлы полетных миссий в среде газебо, работать с терминалом.
Проверил расчёты в среде Gazebo (научился вставлять параметры правильно)

Студент получает от нейронки два лога, один без коррекции, а второй с коррекцией, закидывает их в нейросеть, и расчитывает показатели (отклонения и ошибки), на основе полученных данных делает вывод, что коррекция по маркерам работает.

Студенту нужно объяснить код одного из полетных заданий - то есть он ознакомится командами библиотеки DroneKit.

Студент исправляет ошибки кода полетных заданий, получает объяснения почему код не работал.

## Ответы на вопросы
1.Какие команды библиотеки DroneKit использовались в полётном задании и что они делают?
Ответил.
Так заданием подразумевается, что будут вопросы по коду.

2.Что произойдёт с фактической траекторией дрона, если вместо коэффициента доверия α = 0.4 установить α = 0.9? Обоснуйте, опираясь на формулу коррекции.
Не ответил.
Использовал готовый код, прэтому в формулу не углублялся.

3.Какова физическая природа дрейфа в реальных навигационных системах БПЛА (GPS/IMU)? Почему он накапливается со временем, а не остаётся постоянным?
Не ответил.
После решения лабораторной работы невозможно ответить на этот вопрос, так как там искусственно заданная модель дрейфа.

4.На основании полученных логов (с коррекцией и без), какой из показателей наиболее наглядно демонстрирует эффективность ArUco-коррекции? Почему?
Ответил.
Сам прогонял оба лога через нейросеть и видел метрики (RMSE, e_max, e_avg). Видно, что RMSE снижается в 2-3 раза — это самый показательный результат.

5.С какими ошибками при запуске полётных заданий вы столкнулись и что было их причиной?
Ответил.
Были проблемы из-за того, что версии Python отличаются между тем, что в нейросети и той, что на Virtual Box